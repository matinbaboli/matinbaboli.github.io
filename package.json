{
  "name": "my-portfolio",
  "scripts": {
    "start": "vite --open",
    "build": "vite build",    
    "preview": "vite preview",

    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "motion": "^10.18.0"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "vite": "^5.4.10"
  }
}
