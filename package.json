{
  "name": "my-portfolio",
  "source": "index.html",
  "scripts": {
    "start": "parcel",
    "build": "parcel build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "motion": "^10.18.0"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "parcel": "^2.12.0"
  }
}
